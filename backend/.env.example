# Optional. If unset, the app starts an embedded MongoDB automatically
# (mongodb-memory-server with a persistent data directory) - zero setup.
# MONGODB_URI=mongodb://localhost:27017/weather_app

# PORT=4000
